[project]
name = "claudehopper"
version = "0.2.0"
description = "Bidirectional bridge between CLI Claude and browser Claude"
requires-python = ">=3.11"
dependencies = [
    "websockets>=13.0",
    "aiohttp>=3.9",
]

[project.optional-dependencies]
meridian = [
    "meridian",
]
